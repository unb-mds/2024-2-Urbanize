import { afterAll, beforeAll, describe, expect, it } from 'vitest'

describe('Projects (e2e)', () => {
  beforeAll(() => {
    // This is a beforeAll hook
  })

  afterAll(() => {
    // This is an afterAll hook
  })

  it('Should be able to filter the projects by their data', () => {
    expect(true).toBe(true)
  })
})